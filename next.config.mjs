/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // basePath: "/tasklist",
    // assetPrefix: '/tasklist/',
    // images: {
    //     unoptimized: true,
    // },
};

export default nextConfig;
